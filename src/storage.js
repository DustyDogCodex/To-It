class Storage {
    /* for to do's we will need a separate storage array that will store info about each individual todo as an object */
    static ToDo() {

    }

    /* projects will be an array that contains todos that belooong to that project, an array that contains objects */
    static Projects() {
        
    }
}